export default function DeliveryPage() {
  return (
    <div>
      <h1>Delivery Addresses</h1>
      <button className="p-2 border-2 border-black">Add New Address</button>
      <p>
        Checkout faster by adding one or more shipping addresses to your
        account.
      </p>
    </div>
  );
}
